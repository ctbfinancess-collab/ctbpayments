import React from 'react'; import { StyleSheet, Text, TouchableOpacity, View } from 'react-native'; import { colors, radii, spacing, typography } from '../../theme'; import { formatStatementAmount } from '../../utils/statementUtils';
export default function TransactionCard({ item, onPress }) { const incoming = item.direction === 'entrada'; return <TouchableOpacity activeOpacity={0.78} onPress={onPress} style={styles.card}><View style={[styles.icon, incoming ? styles.incomingBg : styles.outgoingBg]}><Text style={[styles.arrow, incoming ? styles.incoming : styles.outgoing]}>{incoming ? '↓' : '↑'}</Text></View><View style={styles.copy}><Text numberOfLines={1} style={styles.description}>{item.description}</Text><Text numberOfLines={1} style={styles.detail}>{item.counterparty} · {item.time}</Text></View><View style={styles.valueArea}><Text style={[styles.value, incoming ? styles.incoming : styles.outgoing]}>{incoming ? '+' : '-'} R$ {formatStatementAmount(item.amount)}</Text><Text style={styles.status}>{item.status}</Text></View></TouchableOpacity>; }
const styles = StyleSheet.create({ card: { alignItems: 'center', backgroundColor: colors.surface, borderColor: colors.borderSubtle, borderRadius: radii.md, borderWidth: 1, flexDirection: 'row', marginBottom: spacing.sm, minHeight: 72, padding: spacing.md }, icon: { alignItems: 'center', borderRadius: radii.md, height: 40, justifyContent: 'center', width: 40 }, incomingBg: { backgroundColor: 'rgba(46,207,146,0.14)' }, outgoingBg: { backgroundColor: 'rgba(255,138,0,0.14)' }, arrow: { fontSize: 20, fontWeight: '700' }, incoming: { color: colors.success }, outgoing: { color: colors.orange400 }, copy: { flex: 1, marginLeft: spacing.md }, description: { ...typography.bodyMedium, color: colors.textPrimary }, detail: { ...typography.caption, color: colors.textSecondary, marginTop: 3 }, valueArea: { alignItems: 'flex-end', marginLeft: spacing.sm }, value: { ...typography.label }, status: { ...typography.caption, color: colors.textMuted, marginTop: 3 } });
