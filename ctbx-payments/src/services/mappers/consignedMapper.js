import{formatCents}from'./accountMapper';export const mapConsignedProduct=x=>({...x,term:`${x.installmentOptions.join(', ')} parcelas`,rate:x.rateDisplay});export const mapConsignedApplication=x=>{if(!Number.isInteger(x.amountMinor))throw new TypeError('amountMinor must use integer minor units');return{...x,value:formatCents(x.amountMinor),months:String(x.installments),simulated:x.simulated===true}};
