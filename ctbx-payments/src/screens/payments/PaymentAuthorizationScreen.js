import React, { useState } from 'react';
import { Alert, StyleSheet, Text } from 'react-native';
import PaymentLayout from '../../components/payments/PaymentLayout';
import { PaymentField } from '../../components/payments/PaymentForm';
import { ConfirmationButton, OutlineButton } from '../../components/ui';
import { colors, spacing, typography } from '../../theme';
export default function PaymentAuthorizationScreen({ navigation, route }) { const payment = route.params?.payment; const [password, setPassword] = useState(''); const [otp, setOtp] = useState(''); const [cvv, setCvv] = useState(''); const [requested, setRequested] = useState(false); if (!payment) return null; const request = () => { setRequested(true); Alert.alert('Token de demonstração', 'Digite qualquer código de 6 dígitos.'); }; const pay = () => { if (password.length < 4) return Alert.alert('Digite sua senha'); if (payment.installmentData && !/^\d{3}$/.test(cvv)) return Alert.alert('Digite o CVV do cartão'); if (!requested) return request(); if (!/^\d{6}$/.test(otp)) return Alert.alert('Token inválido'); navigation.replace('PaymentReceipt', { payment }); }; return <PaymentLayout navigation={navigation} title="Autorizar pagamento"><Text style={styles.value}>R$ {payment.bill.total}</Text><Text style={styles.recipient}>{payment.bill.beneficiary}</Text><PaymentField keyboardType="numeric" label="Senha" maxLength={6} onChangeText={setPassword} secureTextEntry value={password} />{payment.installmentData ? <PaymentField keyboardType="numeric" label="CVV do cartão" maxLength={3} onChangeText={setCvv} secureTextEntry value={cvv} /> : null}{requested ? <PaymentField keyboardType="numeric" label="Token" maxLength={6} onChangeText={setOtp} value={otp} /> : <OutlineButton onPress={request}>Enviar token</OutlineButton>}<ConfirmationButton onPress={pay} style={styles.pay}>Pagar</ConfirmationButton><Text style={styles.mock}>Autorização, cartão, CVV, OTP e pagamento são simulados.</Text></PaymentLayout>; }
const styles = StyleSheet.create({ value: { ...typography.display, color: colors.textPrimary, marginTop: spacing.lg, textAlign: 'center' }, recipient: { ...typography.body, color: colors.textSecondary, marginBottom: spacing.xxl, marginTop: spacing.sm, textAlign: 'center' }, pay: { marginTop: spacing.md }, mock: { ...typography.caption, color: colors.textMuted, marginTop: spacing.lg, textAlign: 'center' } });
