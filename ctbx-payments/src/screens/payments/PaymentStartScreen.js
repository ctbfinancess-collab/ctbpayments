import React from 'react';
import { Image, StyleSheet, Text, View } from 'react-native';
import PaymentLayout from '../../components/payments/PaymentLayout';
import { Card, SectionTitle, ServiceCard } from '../../components/ui';
import { colors, spacing, typography } from '../../theme';
export default function PaymentStartScreen({ navigation }) { return <PaymentLayout navigation={navigation} title="Pagar conta"><Card style={styles.hero}><Image source={require('../../../assets/legacy/assets_icones_pagamentos.png')} style={styles.heroIcon} /><View style={styles.copy}><Text style={styles.title}>Pagamentos</Text><Text style={styles.subtitle}>Pague boletos e contas com código de barras.</Text></View></Card><SectionTitle style={styles.section} title="Como deseja pagar?" /><View style={styles.grid}><ServiceCard icon={<Text style={styles.icon}>▥</Text>} label="Digitar código" onPress={() => navigation.navigate('PaymentCode', { source: 'manual' })} style={styles.option} /><ServiceCard icon={<Text style={styles.icon}>⌗</Text>} label="Ler código" onPress={() => navigation.navigate('PaymentScanner')} style={styles.option} /><ServiceCard icon={<Text style={styles.icon}>▤</Text>} label="Pagamento parcelado" onPress={() => navigation.navigate('PaymentCode', { source: 'manual', installment: true })} style={styles.option} /></View><Text style={styles.note}>Fluxo original: NavScreen38 → NavScreen5 · Parcelado: NavScreen97</Text></PaymentLayout>; }
const styles = StyleSheet.create({ hero: { alignItems: 'center', flexDirection: 'row', padding: spacing.xl }, heroIcon: { height: 44, tintColor: colors.orange400, width: 44 }, copy: { flex: 1, marginLeft: spacing.lg }, title: { ...typography.heading2, color: colors.textPrimary }, subtitle: { ...typography.body, color: colors.textSecondary, marginTop: 4 }, section: { marginBottom: spacing.md, marginTop: spacing.xxl }, grid: { flexDirection: 'row', flexWrap: 'wrap', gap: spacing.md }, option: { width: '47%' }, icon: { color: colors.purple300, fontSize: 24 }, note: { ...typography.caption, color: colors.textMuted, marginTop: spacing.xxl, textAlign: 'center' } });
