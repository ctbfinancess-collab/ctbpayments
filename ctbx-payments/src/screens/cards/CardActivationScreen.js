import React,{useState}from'react'; import {Alert,StyleSheet,Text,TextInput,View}from'react-native'; import CardScreenLayout from '../../components/cards/CardScreenLayout'; import {Card,ConfirmationButton,PrimaryButton}from'../../components/ui'; import {colors,spacing,typography}from'../../theme'; import {validCardCode,validCardPassword}from'../../utils/cardUtils';
export default function CardActivationScreen({navigation}){const[step,setStep]=useState(0),[value,setValue]=useState(''),[pass,setPass]=useState(''),[confirm,setConfirm]=useState('');const next=()=>{if(step<2&&!validCardCode(value))return Alert.alert('Confira os dados','Digite o código solicitado.');if(step===2&&(!validCardPassword(pass)||pass!==confirm))return Alert.alert('Confira a senha','Use 4 números iguais nos dois campos.');if(step===2)return setStep(3);setValue('');setStep(step+1)};return <CardScreenLayout navigation={navigation} title="Ativar cartão"><View style={s.wrap}><Text style={s.step}>ETAPA {Math.min(step+1,4)} DE 4</Text><Card><Text style={s.title}>{['Confirme seus dados','Código por SMS','Crie a senha do cartão','Cartão ativado'][step]}</Text><Text style={s.copy}>{['Enviaremos um código para o celular cadastrado.','Digite o código de validação recebido.','A senha deve ter quatro números.','Seu cartão está pronto para uso.'][step]}</Text>{step<2&&<TextInput value={value} onChangeText={setValue} keyboardType="number-pad" placeholder={step?'Código SMS':'4 últimos dígitos'} placeholderTextColor={colors.textMuted} style={s.input}/>} {step===2&&<><TextInput value={pass} onChangeText={setPass} secureTextEntry keyboardType="number-pad" placeholder="Nova senha" placeholderTextColor={colors.textMuted} style={s.input}/><TextInput value={confirm} onChangeText={setConfirm} secureTextEntry keyboardType="number-pad" placeholder="Confirmar senha" placeholderTextColor={colors.textMuted} style={s.input}/></>}</Card>{step<3?<PrimaryButton title="Continuar" onPress={next}/>:<ConfirmationButton title="Voltar aos cartões" onPress={()=>navigation.navigate('Cards')}/>}</View></CardScreenLayout>}; const s=StyleSheet.create({wrap:{gap:spacing.lg,padding:spacing.lg},step:{...typography.caption,color:colors.purple400},title:{...typography.heading2,color:colors.textPrimary},copy:{...typography.body,color:colors.textSecondary,marginVertical:spacing.md},input:{backgroundColor:colors.surfaceElevated,borderColor:colors.border,borderRadius:14,borderWidth:1,color:colors.textPrimary,marginTop:spacing.sm,padding:spacing.md}});
