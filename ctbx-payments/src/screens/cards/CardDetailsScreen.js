import React from 'react'; import { StyleSheet, Text, View } from 'react-native'; import CardScreenLayout from '../../components/cards/CardScreenLayout'; import FinancialCard from '../../components/cards/FinancialCard'; import { Card, Divider, EmptyState, ErrorState, LoadingState, OutlineButton, PrimaryButton } from '../../components/ui'; import { getCards } from '../../services/cardService'; import useAsyncResource from '../../hooks/useAsyncResource'; import { colors, spacing, typography } from '../../theme';
export default function CardDetailsScreen({ navigation }) { const { data: cards, error, loading, retry } = useAsyncResource(getCards, []); if (loading) return <CardScreenLayout navigation={navigation} title="Detalhes do cartão"><LoadingState /></CardScreenLayout>; if (error) return <CardScreenLayout navigation={navigation} title="Detalhes do cartão"><ErrorState message="Não foi possível carregar o cartão." onRetry={retry} /></CardScreenLayout>; const card = cards[0]; if (!card) return <CardScreenLayout navigation={navigation} title="Detalhes do cartão"><EmptyState message="Cartão não encontrado." /></CardScreenLayout>; return <CardScreenLayout navigation={navigation} title="Detalhes do cartão"><View style={styles.wrap}><FinancialCard card={card} /><Card style={styles.info}><Row label="Status" value={card.status} /><Divider /><Row label="Bandeira" value={card.brand} /><Divider /><Row label="Final" value={card.lastFour} /></Card><PrimaryButton onPress={() => navigation.navigate('CardStatement')}>Ver extrato</PrimaryButton><OutlineButton onPress={() => navigation.navigate('CardReceipts')}>Comprovantes</OutlineButton></View></CardScreenLayout>; }
const Row = ({ label, value }) => <View style={styles.row}><Text style={styles.label}>{label}</Text><Text style={styles.value}>{value}</Text></View>; const styles = StyleSheet.create({ wrap: { gap: spacing.md, padding: spacing.lg }, info: { gap: spacing.md }, row: { flexDirection: 'row', justifyContent: 'space-between' }, label: { ...typography.body, color: colors.textSecondary }, value: { ...typography.body, color: colors.textPrimary, fontWeight: '700' } });
