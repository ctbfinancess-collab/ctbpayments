import React, { useState } from 'react'; import { Alert, StyleSheet, Switch, Text, View } from 'react-native'; import CardScreenLayout from '../../components/cards/CardScreenLayout'; import { Card } from '../../components/ui'; import { setBlocked as updateBlockedState } from '../../services/cardService'; import { colors, spacing, typography } from '../../theme';
export default function CardSecurityScreen({ navigation }) { const [blocked, setBlocked] = useState(false); const confirm = (value) => Alert.alert(value ? 'Bloquear cartão?' : 'Desbloquear cartão?', value ? 'Compras serão interrompidas.' : 'O cartão voltará a aceitar compras.', [{ text: 'Cancelar' }, { text: 'Confirmar', onPress: async () => { try { await updateBlockedState(value); setBlocked(value); } catch { Alert.alert('Serviço indisponível', 'O bloqueio do cartão ainda não está configurado.'); } } }]); return <CardScreenLayout navigation={navigation} title="Bloquear cartão"><View style={styles.wrap}><Card elevated={false} style={styles.row}><View style={styles.text}><Text style={styles.title}>{blocked ? 'Cartão bloqueado' : 'Cartão desbloqueado'}</Text><Text style={styles.copy}>Bloqueio temporário. A alteração real depende da API original.</Text></View><Switch value={blocked} trackColor={{ false: colors.slate500, true: colors.orange500 }} thumbColor={colors.textPrimary} onValueChange={confirm} /></Card></View></CardScreenLayout>; }
const styles = StyleSheet.create({ wrap: { padding: spacing.lg }, row: { alignItems: 'center', backgroundColor: 'rgba(12, 43, 76, 0.72)', borderColor: 'rgba(92, 142, 220, 0.10)', borderWidth: 1, flexDirection: 'row', shadowColor: '#000000', shadowOffset: { width: 0, height: 8 }, shadowOpacity: 0.14, shadowRadius: 24, elevation: 4 }, text: { flex: 1, paddingRight: spacing.md }, title: { ...typography.heading3, color: colors.textPrimary }, copy: { ...typography.caption, color: colors.textSecondary, marginTop: 6 } });
