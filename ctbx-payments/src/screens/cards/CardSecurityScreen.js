import React,{useState}from'react'; import {Alert,StyleSheet,Switch,Text,View}from'react-native'; import CardScreenLayout from '../../components/cards/CardScreenLayout'; import {Card}from'../../components/ui'; import {colors,spacing,typography}from'../../theme';
export default function CardSecurityScreen({navigation}){const[blocked,setBlocked]=useState(false);return <CardScreenLayout navigation={navigation} title="Bloquear cartão"><View style={s.wrap}><Card style={s.row}><View style={s.text}><Text style={s.title}>{blocked?'Cartão bloqueado':'Cartão desbloqueado'}</Text><Text style={s.copy}>Bloqueio temporário. A alteração real depende da API original.</Text></View><Switch value={blocked} trackColor={{false:colors.slate500,true:colors.orange500}} thumbColor={colors.textPrimary} onValueChange={v=>Alert.alert(v?'Bloquear cartão?':'Desbloquear cartão?',v?'Compras serão interrompidas.':'O cartão voltará a aceitar compras.',[{text:'Cancelar'},{text:'Confirmar',onPress:()=>setBlocked(v)}])}/></Card></View></CardScreenLayout>};const s=StyleSheet.create({wrap:{padding:spacing.lg},row:{alignItems:'center',flexDirection:'row'},text:{flex:1,paddingRight:spacing.md},title:{...typography.heading3,color:colors.textPrimary},copy:{...typography.caption,color:colors.textSecondary,marginTop:6}});
