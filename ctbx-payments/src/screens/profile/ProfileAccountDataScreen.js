import React from'react';import{StyleSheet,Text}from'react-native';import ProfileScreenLayout from'../../components/profile/ProfileScreenLayout';import ProfileRow from'../../components/profile/ProfileRow';import{Card}from'../../components/ui';import{MOCK_ACCOUNT}from'../../data/profileMockData';import{colors,typography}from'../../theme';
export default function ProfileAccountDataScreen({navigation}){const a=MOCK_ACCOUNT;return <ProfileScreenLayout navigation={navigation} title="Dados da conta"><Card style={s.card}><ProfileRow label="Instituição" value={a.bank}/><ProfileRow label="Agência" value={a.agency}/><ProfileRow label="Conta" value={a.number}/><ProfileRow label="Tipo" value={a.type}/><ProfileRow label="Status" value={a.status}/></Card><Text style={s.note}>Agência, conta e status são mocks até a sessão bancária original ser conectada.</Text></ProfileScreenLayout>}const s=StyleSheet.create({card:{paddingBottom:0,paddingTop:0},note:{...typography.caption,color:colors.orange500}});
