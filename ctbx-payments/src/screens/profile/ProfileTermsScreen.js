import React, { useState } from 'react'; import { StyleSheet, Text, TouchableOpacity } from 'react-native'; import ProfileScreenLayout from '../../components/profile/ProfileScreenLayout'; import { Card, EmptyState, ErrorState, LoadingState, ModalSheet, OutlineButton } from '../../components/ui'; import { getTerms } from '../../services/profileService'; import useAsyncResource from '../../hooks/useAsyncResource'; import { colors, spacing, typography } from '../../theme';
export default function ProfileTermsScreen({ navigation }) { const [selected, setSelected] = useState(null); const { data: terms, error, loading, retry } = useAsyncResource(getTerms, []); if (loading) return <ProfileScreenLayout navigation={navigation} title="Meus termos"><LoadingState /></ProfileScreenLayout>; if (error) return <ProfileScreenLayout navigation={navigation} title="Meus termos"><ErrorState message="Não foi possível carregar os termos." onRetry={retry} /></ProfileScreenLayout>; return <ProfileScreenLayout navigation={navigation} title="Meus termos">{terms.length ? terms.map((term) => <TouchableOpacity key={term.key} onPress={() => setSelected(term)}><Card style={styles.row}><Text style={styles.icon}>§</Text><Text style={styles.label}>{term.label}</Text><Text style={styles.arrow}>›</Text></Card></TouchableOpacity>) : <EmptyState message="Nenhum termo disponível." />}<ModalSheet visible={Boolean(selected)} title={selected?.label} onClose={() => setSelected(null)}><Text style={styles.copy}>O texto integral é carregado pelo endpoint termos/texto usando a chave “{selected?.key}”. O conteúdo não foi inventado nesta reconstrução.</Text><OutlineButton onPress={() => setSelected(null)} style={styles.button}>Fechar</OutlineButton></ModalSheet></ProfileScreenLayout>; }
const styles = StyleSheet.create({ row: { alignItems: 'center', flexDirection: 'row' }, icon: { color: colors.purple400, fontSize: 24 }, label: { ...typography.body, color: colors.textPrimary, flex: 1, marginLeft: spacing.md, fontWeight: '700' }, arrow: { color: colors.textSecondary, fontSize: 28 }, copy: { ...typography.body, color: colors.textSecondary, lineHeight: 23 }, button: { marginTop: spacing.lg } });
