import React from'react';import{StyleSheet,Text}from'react-native';import ProfileScreenLayout from'../../components/profile/ProfileScreenLayout';import ProfileRow from'../../components/profile/ProfileRow';import{Card}from'../../components/ui';import{MOCK_PROFILE}from'../../data/profileMockData';import{colors,spacing,typography}from'../../theme';
export default function ProfilePersonalDataScreen({navigation}){const p=MOCK_PROFILE,a=p.address;return <ProfileScreenLayout navigation={navigation} title="Dados pessoais"><Text style={s.note}>Dados somente para consulta, como no perfil recuperado.</Text><Card style={s.card}><ProfileRow label="Nome completo" value={p.name}/><ProfileRow label="CPF/CNPJ" value={p.document}/><ProfileRow label="E-mail" value={p.email}/><ProfileRow label="Celular" value={p.phone}/><ProfileRow label="Data de nascimento" value={p.birthDate}/><ProfileRow label="Estado civil" value={p.maritalStatus}/></Card><Text style={s.section}>Endereço</Text><Card style={s.card}><ProfileRow label="CEP" value={a.zip}/><ProfileRow label="Endereço" value={`${a.street}, ${a.number}`}/><ProfileRow label="Complemento" value={a.complement||'—'}/><ProfileRow label="Bairro" value={a.district}/><ProfileRow label="Cidade/UF" value={`${a.city}/${a.state}`}/></Card></ProfileScreenLayout>}const s=StyleSheet.create({note:{...typography.caption,color:colors.orange500},card:{paddingBottom:0,paddingTop:0},section:{...typography.heading3,color:colors.textPrimary,marginTop:spacing.md}});
